interface:
  display_name: "Voice to Obsidian"
  short_description: "Transcribe uploaded audio into Obsidian transcript and summary notes"
  default_prompt: "Use $voice-to-obsidian to transcribe this uploaded audio file locally with FunASR, then save transcript and intelligent-summary Markdown notes into the configured Obsidian vault."
policy:
  allow_implicit_invocation: true
